<div class="container">
    <div class="row">
        @if (ViewBag.QrCodeImage != null)
        {
            <h3>@ViewBag.PetName</h3>
            <img src="@ViewBag.QrCodeImage" alt="QR Code" style="height:300px; width:300px" />
        }
    </div>
</div>